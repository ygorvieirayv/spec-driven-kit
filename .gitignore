# Segredos — NUNCA commitar (ver engineering-standards.md → "Sempre")
.env
.env.*
!.env.example
*.pem
*.key
secrets/
.secrets

# Dependências / build (genérico — ajuste ao stack do projeto)
node_modules/
dist/
build/
.next/
.cache/
__pycache__/
*.py[cod]
.venv/
venv/
target/
vendor/

# Logs e temporários
*.log
logs/
tmp/
.DS_Store
Thumbs.db

# Editores / IDE
.idea/
.vscode/
*.swp

# Saídas de ferramentas de análise (ex.: graphify — grafo de conhecimento gerado;
# graph.json carrega strings extraídas do código: manter fora do repo e do scan de segredos)
graphify-out/

# Adaptadores gerados (a fonte canônica continua em .claude/commands/)
.opencode/commands/sdk-*.md
